//we discuss in HTML till tables
CSS: means cascading style sheets. Therse are usred to include styling/desining into web pages.
there are three ways to achieve CSS.
1.Inline : Inline css means in the line of  gtml element, style will applied as a attribute.
<h1 style ="color:red"> something </h1>
2.Internal :Internal stylig means apply style as a element 
<style>
selector
{
    property-name:valuye;
}
/*selectors can be directly tagnames like h1,div,etc  or classname like .class, .class2 etc., or id names like #id1,#id2 etc3.
External CSS
we can write css in seprate file with dot css extension and link that css 